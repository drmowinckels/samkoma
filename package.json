{
  "name": "samkoma",
  "private": true,
  "description": "Repo-wide tooling (formatting). Each app has its own package under api/, web/, cli/, client/.",
  "scripts": {
    "format": "prettier --write \"**/*.{ts,tsx,css}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,css}\""
  },
  "devDependencies": {
    "prettier": "3.4.2"
  }
}
